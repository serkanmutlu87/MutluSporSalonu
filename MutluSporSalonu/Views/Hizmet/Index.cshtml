@*
@file        Index.cshtml
@description Sistemde tanımlı tüm hizmetlerin listelendiği ekrandır.
             Admin kullanıcılar "Hizmet Ekle / Düzenle / Sil" işlemlerini görür.
             Normal üyeler sadece "Detay" görüntüleyebilir.

@course      BSM 311 Web Programlama
@assignment  Dönem Projesi – MutluSporSalonu
@date        20.12.2025
@author      D255012008 - Serkan Mutlu
*@

@model IEnumerable<MutluSporSalonu.Models.Hizmet>

@{
    ViewData["Title"] = "Hizmetler";

    // Kullanıcının Admin rolünde olup olmadığı kontrol edilir (butonlar buna göre gösterilir)
    bool adminMi = User.IsInRole("Admin");
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Hizmetler</h2>

        @* Admin ise "Hizmet Ekle" butonu görünür *@
        @if (adminMi)
        {
            <a asp-action="Create" class="btn btn-primary">
                Hizmet Ekle
            </a>
        }
    </div>

    @if (Model == null || !Model.Any())
    {
        <div class="alert alert-info">
            Sistemde tanımlı hizmet bulunmamaktadır.
            @if (adminMi)
            {
                <a asp-action="Create">Yeni hizmet eklemek için tıklayın.</a>
            }
        </div>
    }
    else
    {
        <table class="table table-striped table-bordered table-hover">
            <thead class="table-light">
                <tr>
                    <th>Hizmet Adı</th>
                    <th>Süre (dk)</th>
                    <th>Ücret (₺)</th>
                    <th>Salon</th>

                    @* Admin ise daha fazla işlem butonu olduğu için kolon genişliği artırılır *@
                    <th style="width:@(adminMi ? "190px" : "90px");">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var hizmet in Model)
                {
                    <tr>
                        <td>@hizmet.HizmetAdi</td>
                        <td>@hizmet.HizmetSureDakika</td>
                        <td>@hizmet.HizmetUcret</td>
                        <td>@hizmet.SporSalonu?.SalonAdi</td>
                        <td>
                            <div class="btn-group" role="group">
                                @* Her kullanıcı Detay sayfasını görebilir *@
                                <a asp-action="Details" asp-route-id="@hizmet.HizmetID" class="btn btn-sm btn-info">
                                    Detay
                                </a>

                                @* Admin ise Düzenle/Sil butonları eklenir *@
                                @if (adminMi)
                                {
                                    <a asp-action="Edit" asp-route-id="@hizmet.HizmetID" class="btn btn-sm btn-warning">
                                        Düzenle
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@hizmet.HizmetID" class="btn btn-sm btn-danger">
                                        Sil
                                    </a>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
